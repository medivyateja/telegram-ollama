<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
    <div class="text-center">
        <h1 class="text-6xl font-bold text-gray-800 mb-4">500</h1>
        <p class="text-xl text-gray-600 mb-4">Internal Server Error</p>
        <p class="text-gray-500 mb-8">Something went wrong on our end. Please try again later.</p>
        <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
            <pre class="text-left bg-gray-200 p-4 rounded-lg mt-4 overflow-x-auto">
                <%= error.stack %>
            </pre>
        <% } %>
        <a href="/" class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded transition duration-300">
            Go Back Home
        </a>
    </div>
</body>
</html>