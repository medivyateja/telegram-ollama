<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Hexa</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100 min-h-screen">
    <!-- Navigation Bar -->
    <nav class="bg-white shadow-lg">
        <div class="container mx-auto px-6 py-3">
            <div class="flex items-center justify-between">
                <div class="text-xl font-semibold text-gray-700">
                    <a href="/">Hexa</a>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="text-gray-600">
                        Welcome, <span class="font-semibold"><%= user.username %></span>
                    </div>
                    <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition duration-200 ease-in-out">
                        Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Content -->
    <div class="container mx-auto px-6 py-8">
        <% if (messages.success) { %>
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
                <%= messages.success %>
            </div>
        <% } %>
        
        <% if (messages.error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
                <%= messages.error %>
            </div>
        <% } %>
        
        <div class="max-w-3xl mx-auto">
            <div class="bg-white shadow rounded-lg p-6 mb-6">
                <h2 class="text-2xl font-bold mb-4">User Profile</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <p class="text-gray-600">Username</p>
                        <p class="font-semibold"><%= user.username %></p>
                    </div>
                    <div>
                        <p class="text-gray-600">Account Created</p>
                        <p class="font-semibold"><%= new Date(user.createdAt).toLocaleDateString() %></p>
                    </div>
                </div>
            </div>
            
            <!-- Connected Services -->
            <div class="bg-white shadow rounded-lg p-6">
                <h2 class="text-2xl font-bold mb-4">Connected Services</h2>
                
                <!-- Telegram -->
                <div class="border-b border-gray-200 pb-4 mb-4">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center">
                            <div class="w-12 h-12 flex items-center justify-center bg-blue-500 text-white rounded-full mr-4">
                                <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                                    <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.562 8.198l-2.02 9.376c-.151.646-.563.793-1.151.499l-3.505-2.458-1.225 1.157c-.145.129-.334.173-.566.173H9.1c-.145 0-.27-.043-.338-.129-.068-.086-.085-.193-.034-.335l1.449-4.568-4.542-1.173c-.567-.172-.567-.567.034-.739l7.151-2.458c.601-.172 1.151.129 1.084.739z" />
                                </svg>
                            </div>
                            <div>
                                <h3 class="font-semibold text-lg">Telegram</h3>
                                <p class="text-sm text-gray-600">
                                    <% if (user.telegramConnected) { %>
                                        Connected
                                    <% } else { %>
                                        Not connected
                                    <% } %>
                                </p>
                            </div>
                        </div>
                        <div>
                            <% if (user.telegramConnected) { %>
                                <a href="/telegram/connect" class="text-blue-500 hover:text-blue-700 mr-4">View</a>
                                <form action="/telegram/disconnect" method="POST" class="inline">
                                    <button type="submit" class="text-red-500 hover:text-red-700">Disconnect</button>
                                </form>
                            <% } else { %>
                                <a href="/telegram/connect" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded transition">
                                    Connect
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>
                
                <!-- Telegram Message Monitor -->
                <div class="border-b border-gray-200 pb-4 mb-4">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center">
                            <div class="w-12 h-12 flex items-center justify-center bg-indigo-500 text-white rounded-full mr-4">
                                <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
                                </svg>
                            </div>
                            <div>
                                <h3 class="font-semibold text-lg">Message Monitor</h3>
                                <p class="text-sm text-gray-600">Track private messages</p>
                            </div>
                        </div>
                        <div>
                            <a href="/telegram/monitor" class="bg-indigo-500 hover:bg-indigo-600 text-white px-4 py-2 rounded transition">
                                Open Monitor
                            </a>
                        </div>
                    </div>
                </div>
                
                <!-- Add more services here as needed -->
            </div>
        </div>
    </div>
</body>
</html>